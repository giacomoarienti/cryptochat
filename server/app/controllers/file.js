// TODO: to implement
const downloadFile = (req, res) => {
    req.params.fileId;

    //check if file exists

    //get file content

    //delete file

    return res.status(200).send();
};

module.exports = { downloadFile };